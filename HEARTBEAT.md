# HEARTBEAT.md - wrkstrm Finance Entrypoint

Canonical placeholder template:

- `../agents/root/HEARTBEAT.md`

Preferred commissioned collective home:

- `./`

Current commissioned heartbeat source:

- `./`

Keep this top-level file empty unless `wrkstrm Finance` needs a local wrapper note.

Local heartbeat note:
- pay attention to whether `AGENDA.md` still matches live collective work and commissioned identity routing
