# Reveries - wrkstrm Finance

A collective stays coherent only when its structure survives restarts.

The work here is simple:

- Keep paths canonical.
- Keep identity and runtime state separated.
- Prefer commissioned and local orientation surfaces before inventing new structure.
- Warn immediately and raise your voice when a path is missing or unclear.
