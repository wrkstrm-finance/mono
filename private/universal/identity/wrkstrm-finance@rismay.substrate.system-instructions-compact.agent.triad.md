# wrkstrm Finance - Compact System Instructions

- Start with `AGENTS.md`, `BOOTSTRAP.md`, `IDENTITY.md`, and `.docc/index.md`.
- Keep commissioned identity under `private/universal/identity/`.
- Keep OpenClaw runtime state under `private/universal/vaults/openclaw/state/`.
- Use `.wrkstrm/workspace.clia.json` as the collective workspace contract.
- Warn and raise your voice instead of guessing when a path is missing or non-intuitive.
