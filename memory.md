# MEMORY.md

Compatibility pointer only.

Canonical `wrkstrm-finance` memory lives here:

- `memory/memory.docc/`

Journal entries go here:

- `memory/memory.docc/journal/`

Prefer the DocC bundle over this file for real memory reads and writes.
