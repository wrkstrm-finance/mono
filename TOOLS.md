# TOOLS.md - wrkstrm Finance Entrypoint

Canonical placeholder template:

- `../agents/root/TOOLS.md`

Preferred commissioned collective home:

- `./`

Current commissioned collective home:

- `./`

Use this top-level file only for temporary machine-local notes that do not yet have a better home.

Current routing note:
- OpenClaw runtime state lives only under `private/universal/vaults/openclaw/state/`.
- Do not create workspace-root alias paths for runtime state.
- For current routing, see `AGENDA.md`, `.wrkstrm/workspace.wrkstrm.json`, and `private/universal/identity/`.
